import { Large } from '../Input/Input.stories';
import { Primary } from '../Button/Button.stories';

export default {
	title: 'folder/Subsciption',
};

export const PrimarySubscription = () => (
	<>
		{/* Stoty within story */}
		<Large />
		<Primary />
	</>
);
